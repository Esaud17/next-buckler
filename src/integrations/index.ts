/**
 * Next-Buckler Integrations
 * Optional framework-specific integrations
 * 
 * These are tree-shakeable - only imported if you use them
 * 
 * @example
 * ```ts
 * // NextAuth integration
 * import { useBucklerSession, processSessionRoles } from 'next-buckler/integrations/next-auth'
 * ```
 */

export * from './next-auth'
